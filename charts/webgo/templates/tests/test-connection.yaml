apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webgo.fullname" . }}-test-connection"
  labels:
    {{- include "webgo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webgo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
